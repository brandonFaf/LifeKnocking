<%= form_for(@extra, :url => {:action=>"create", :controller=>"extra"}, :method => :post) do |f| %> 
 <% if @extra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@extra.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @extra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, size: "50" %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, size: "90x10" %>
  </div>
  <div class="field">
    <%= f.label :episode_id %><br>
    <%= f.text_field :episode_id, size: "50" %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
  <%= link_to 'Back', Episode.find(@extra.episode_id).podcast %>
