<%= form_for(@suggestion) do |f| %>
  <% if @suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggestion.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% @suggestion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<div class = "row">
		<div class="field  col-md-7 col-sm-7  <%= 'form-group has-error' if @suggestion.errors[:name].count > 0 %>">
	    	<label class="control-label">Your Name</label><br>
	    	<%= f.text_field :name, size: "50", class: "form-control"%>
	  	</div>
  	</div>
  	<div class="row">
		<div class="field col-md-7 col-sm-7  <%= 'form-group has-error' if @suggestion.errors[:title].count > 0 %>">
	    <%= f.label :title, class: "control-label"%><br>
	    <%= f.text_field :title, size: "50", class: "form-control"%>
	  </div>
  </div>
  <div class="row">
	  <div class="field col-md-7 col-sm-7 <%= 'form-group has-error' if @suggestion.errors[:description].count > 0 %>">
	    <%= f.label :description, class: "control-label" %><br>
	    <%= f.text_area :description, size: "80x10", class: "form-control" %>
	  </div>
  </div>
   <br>
  <div class="action">
    <%= f.submit "Create suggestion", class: "btn btn-primary" %>
    <%= link_to 'Cancel', posts_path, class: "btn btn-primary" %>
  </div>
  <br>
<% end %>
